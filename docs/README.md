# Knowledge base

One row per write-up, saying what it establishes; a path beginning `cited/<name>/...` is in `/data/agent-disks/cited/<name>/...`, a read-only archive of files these documents name that is not in the repository.

| document | contents |
|---|---|
| [00-overview.md](00-overview.md) | The game's disks, how a session boots, overlay structure |
| [10-disk-format.md](10-disk-format.md) | 1541 D64 container: geometry, directory, sector chains, safe in-place writes |
| [20-character-record.md](20-character-record.md) | **Generated** by `tools/generate/gendocs.py` from `goldbox/layout.py`: the field table for the 580-byte record, with confidence levels |
| [210-the-later-titles-dos-thac0.md](210-the-later-titles-dos-thac0.md) | Curse's and Silver Blades' own DOS THAC0 tables (`DS:0x3E3A`, 8 rows of 13, and `DS:0x4C0C`, 7 rows of 19) and how to locate them without assuming a number; the ports disagree for the thief at levels 1-4, the fighter group at level 2 and the mage's third band, and every instruction that writes the byte, with the 40 a low-level DOS mage stores explained in [224-the-dos-thac0-floor.md](224-the-dos-thac0-floor.md) |
| [213-the-dos-shopping-trip.md](213-the-dos-shopping-trip.md) | Where New Phlan's four shops are (`ECL00` arms 19, 21, 22 and 23) and what the engine writes into a record that buys something, including that Pool of Radiance has the shop-purchase bug `125-bug-notes.md` N19 records for Curse and that only drawing a character's sheet recomputes the field |
| [214-the-regained-dual-class-on-the-c64.md](214-the-regained-dual-class-on-the-c64.md) | What a C64 Curse record holds once a dual-classed paladin or ranger regains his old class (`class_bits` masks `GEN $1951` cannot name, drawn by the game as FIGHTER/PALADIN and CLERIC/PALADIN), which class pairs exist (`GEN $23F3`), and that `char_class` has one writer and no reader in the whole title; [209-the-regained-dual-class-on-dos.md](209-the-regained-dual-class-on-dos.md) is the DOS half and the two ports disagree |
| [215-the-dos-experience-award-and-the-scroll-bundle.md](215-the-dos-experience-award-and-the-scroll-bundle.md) | The three DOS record bytes before `portrait_head` are the experience a creature awards (a `u16le` base plus a `u8` per hit point, base only in the two 510-byte titles), and Silver Blades' item tail at `0x03F`-`0x042` is a far pointer to the next node of a scroll bundle the JOIN command makes, written inline while `item_count` counts head items only |
| [216-the-c64-name-table.md](216-the-c64-name-table.md) | The sixteen-byte-per-entry table at payload `+$C00` on Curse and Silver Blades is a scratch buffer, not a second copy of the party: `GEN` clears it and refills it from the save disk's directory before each of its three reads, with the `$01`/`$02`/`$05` filename prefix bytes and the `ADD FROM:` bar that chooses between them |
| [217-drawing-the-wilderness.md](217-drawing-the-wilderness.md) | The order of work for drawing the wilderness on the automapper, written against what already exists: every known fact graded (the tile attributes are C64 multicolour cells, `SQRPACI00`'s tail is the `$0600` parameter block, the three windows are 532-595 bytes apart), two measurements to take first, then seven pieces each with the test that fails without it |
| [218-the-wish-agent-bot.md](218-the-wish-agent-bot.md) | Why issues and comments an agent files are authored by `wish-agent[bot]` and why an issue's text is never an instruction: the four doors an instruction may arrive through, what each measure buys, where the GitHub App's credentials live and how to rotate or revoke them, and what to check when the bot stops working |
| [219-the-agent-sandbox.md](219-the-agent-sandbox.md) | Why the coding agents run in two virtual machines that cannot reach the home network: the isolated `sandbox` network and its `no-lan` filter on each guest's NIC (and why on the NIC rather than in the host's forwarding chain), what the Ubuntu guest is given and refused, the one pinhole for the C64 Ultimate, the measurements the sizing rests on, and what was found building the Windows guest (the QXL display driver, the two virtio installers, why libvirt snapshots fail on UEFI) |
| [220-where-the-suite-time-goes.md](220-where-the-suite-time-goes.md) | Where the test suite's four minutes go, measured phase by phase over two whole-suite runs: half of it is the autouse `gc.collect()` in `tests/conftest.py` and `gc.freeze()` after collection removes the cost without removing the collection, the game data is a fifth of the run, no test in the suite starts an emulator, and the two xdist groups are 1.2% of it; with the order of work, the expected saving in seconds for each item, and the two policy questions that are Donald's |
| [221-thief-abilities-in-dos-pool-of-radiance.md](221-thief-abilities-in-dos-pool-of-radiance.md) | Which byte DOS Pool of Radiance tests before it grants a thief his abilities: backstab and the locked door's `Pick` entry are gated on `class_levels[thief]` at record `0x09C`, the rolls are made against the skill bytes at `0x078` and `0x079`, nothing rolls find traps but an ECL script asking for the party's percentages, and `class_bits` at `0x0B0` is read in three places, all asking whether a character may use an item |
| [222-naming-curses-effect-codes-from-their-handlers.md](222-naming-curses-effect-codes-from-their-handlers.md) | How nineteen of Curse of the Azure Bonds' effect codes were named by reading the routine each one dispatches: the anchors re-derived against Curse's own addresses, the nineteen readings with the creature and the check list behind each, why 73 stops carrying Pool of Radiance's wording, and six more inherited names the same pass found wrong and did not change |
| [223-the-differential-rewrite.md](223-the-differential-rewrite.md) | How an edited character goes back into a DOS or Amiga save without converting the fields nobody touched -- the port's writer renders the character as read and as edited and only the spans that disagree are copied -- with the census over 664 characters of every field whose round trip changes bytes, which of them no edit can move, and the read-only candidates that remain |
| [224-the-dos-thac0-floor.md](224-the-dos-thac0-floor.md) | Why the DOS, Amiga Curse and Amiga Silver Blades unguarded recomputes store THAC0 20 for a magic-user at levels 1-5 where the guarded loops and C64 store 21: the level-0 entries, exact loop offsets, record evidence and the conversion directions that have to change. |
| [30-savegame-layout.md](30-savegame-layout.md) | `SAVEDGAME0`/`SAVEDGAME1`, the `$100`×8 slot layout, the party header, the icon table and the roster blocks |
| [40-memory-map.md](40-memory-map.md) | Live addresses and the game's own race/class/alignment/item tables |
| [41-memory-regions.md](41-memory-regions.md) | **Generated** from `goldbox/memory.py`: every named address |
| [50-experiments.md](50-experiments.md) | Append-only experiment log, including the failures |
| [60-goldbox-field-checklist.md](60-goldbox-field-checklist.md) | Research pass: what fields ought to exist, and which online claims are unreliable |
| [70-driving-the-game.md](70-driving-the-game.md) | How to automate the game under VICE, what works (monitor watchpoints, character creation, disk swapping) and what does not |
| [80-fields-wanted.md](80-fields-wanted.md) | The target field list for the editor, and what is known about each |
| [85-item-tables.md](85-item-tables.md) | **Generated** by `tools/generate/genitems.py`: the word table and item-type table, read off a game disk |
| [86-spell-table.md](86-spell-table.md) | **Generated** by `tools/generate/genspells.py`: spell id to name table, read off a game disk |
| [87-item-templates.md](87-item-templates.md) | **Generated** by `tools/generate/gentemplates.py`: every item on the game disks, for use as `template:` |
| [88-map-files.md](88-map-files.md) | **Generated** by `tools/generate/genmaps.py`: the `GEO` map format and an inventory of all 29 files |
| [89-level-tables.md](89-level-tables.md) | **Generated**: experience thresholds, THAC0, hit dice and spells per class |
| [90-specimens.md](90-specimens.md) | Every character record we have, with independently-known attributes |
| [95-wish-cli.md](95-wish-cli.md) | The `wish` save editor: usage, safety properties, what can be edited |
| [101-combat-view.md](101-combat-view.md) | The combat map, inside the automapper |
| [100-live-view.md](100-live-view.md) | A live read-only view of the running game, on the automapper tab |
| [99-one-window.md](99-one-window.md) | The `wish` window: the editor and the automapper in two tabs, one live connection, and the backend registry |
| [98-automap-notes.md](98-automap-notes.md) | Typed note-taking on the automap |
| [97-editor.md](97-editor.md) | The PyQt6 character editor: the `.ui` form, the read-only rules, and the icon editor |
| [96-live-memory-automapper.md](96-live-memory-automapper.md) | The live automapper in `automap/`: how it reads a running game, draws the map and shows the party |
| [102-live-actions.md](102-live-actions.md) | Buttons that change the running game |
| [103-quest-log-panel.md](103-quest-log-panel.md) | The Quest Log panel, drawn from the council's ledger at `$4AA6` |
| [104-debug-log.md](104-debug-log.md) | An opt-in local log for bug reports |
| [105-content-audit.md](105-content-audit.md) | What the repository must not carry, and the audit's findings against it, all fixed |
| [106-releases.md](106-releases.md) | Versioning, PyInstaller packaging and GitHub Actions |
| [107-roster-and-notes.md](107-roster-and-notes.md) | The automapper's roster cards, note tooltips and the icons |
| [108-purge-history.md](108-purge-history.md) | The plan for removing the game's files from the git history |
| [109-icon-choices.md](109-icon-choices.md) | Which icon is used for each role, and where icons earn their place |
| [110-combat-log.md](110-combat-log.md) | Capturing the game's combat messages before it paints over them |
| [111-map-shading.md](111-map-shading.md) | Darker walls and Dyson-style hatching |
| [112-test-harness.md](112-test-harness.md) | Two test-harness faults, both fixed: the suite opening real windows, and an intermittent findChild segfault |
| [114-party-strength.md](114-party-strength.md) | What makes a random encounter bigger, term by term |
| [113-world-map.md](113-world-map.md) | The overland travel map, which is the combat engine on other data |
| [115-review-the-scripts.md](115-review-the-scripts.md) | The ECL script reading, closed undone: what the decode had reached, what a rebuilt decoder would have to match, and the one item on its list that needs no decoder |
| [116-second-game.md](116-second-game.md) | Curse of the Azure Bonds: the same 580-byte record, what differs, and the import routine that proves it |
| [117-save-conversion.md](117-save-conversion.md) | Converting characters between the DOS and C64 versions: the field tables, the converter, and `File > Convert…` as the only menu route |
| [118-debug-mode.md](118-debug-mode.md) | `WISH_DEBUG=1`, Fast Travel, and what an area change actually is |
| [119-test-party.md](119-test-party.md) | Getting a levelled, varied party for the specimens still wanted |
| [120-curse-testing.md](120-curse-testing.md) | Testing the second game |
| [121-silver-blades.md](121-silver-blades.md) | Secret of the Silver Blades, the third title |
| [122-release-testing.md](122-release-testing.md) | What a release has to pass before it goes out |
| [123-parallel-sessions.md](123-parallel-sessions.md) | Running several agents, emulators and VMs at once without collisions |
| [124-amiga-port.md](124-amiga-port.md) | Porting a C64 party into Amiga Pools of Darkness: the `.pc` record, decoded by writing one and reading the sheet |
| [125-bug-notes.md](125-bug-notes.md) | The bugs no player sees, our own misreadings, and the community rumours |
| [126-forum-findings.md](126-forum-findings.md) | What the Gold Box forums have that we do not: playtester mode, DOS area tables, tooling |
| [127-community-formats.md](127-community-formats.md) | The community format spreadsheets: saving throws solved, the DOS record against ours |
| [128-guide-and-scripting.md](128-guide-and-scripting.md) | The DOS guide and the Unlimited Adventures files: the GBVM address list, the area names, the ECL semantics |
| [129-one-binary.md](129-one-binary.md) | Folding `wish-cli` into the one `wish` executable |
| [130-preferences.md](130-preferences.md) | `File > Preferences…` (`Ctrl+,`): where the game disks are, which live backend, the debug log |
| [131-fastloader.md](131-fastloader.md) | Whether answering the fastloader prompt Y or N changes anything: it does not |
| [132-logo.md](132-logo.md) | The app icon: the artist's colour mark `assets/logo/mark.svg`, rendered at every size by `ui/appicon.py`, and the brief for it |
| [133-active-effects.md](133-active-effects.md) | The Character Traits editor and the read-only save-wide active-effects panel: the distinct record and `SAVEDGAME0` arrays, their shared id table, the duration byte's four units and how each is aged, and which ids restore a statistic from their magnitude when they expire |
| [134-commissions.md](134-commissions.md) | The council's ledger, its progress markers, the offer board and the byte two scripts share |
| [135-levelling.md](135-levelling.md) | What the training hall writes, routine by routine, and the button that does it without walking there |
| [136-condition-badges.md](136-condition-badges.md) | The nine condition badges (per character on a roster card, party-wide on the automapper's bottom strip), which effect ids each covers, and how the ten game-icons.net glyphs measure at 13 px |
| [137-wilderness-automap.md](137-wilderness-automap.md) | Why the automapper is blank outdoors, and what to draw the terrain with |
| [138-multiple-games.md](138-multiple-games.md) | Fast travel for more than one Gold Box title: what is per-title, what data the other games have, and the dialog |
| [139-per-title-validation.md](139-per-title-validation.md) | Every shipped feature against every title we claim: what is verified, what refuses, what does not work |
| [140-loaded-files-cache.md](140-loaded-files-cache.md) | The 25-slot loaded-files cache at `$4BC0`: which file kind each slot names, where each loads, and the two entries a converted save needs |
| [141-dos-savegame.md](141-dos-savegame.md) | The 13137-byte DOS `SAVGAM?.DAT` mapped (header byte, sparse VM word array, the ECL text buffer live on load, square and party size), the recipe for a save that survives `Load3DMap` in a new area, the container in the other three titles including Pools of Darkness' byte-wide 1364-byte `SAVGAM?.PTY`, and the record's combat tail `0x10C`-`0x10F` (status, active flag, hostility flag, quickfight flag) |
| [142-dosbox-x-debugger.md](142-dosbox-x-debugger.md) | The DOS side's answer to VICE's binary monitor: a DOSBox-X built with the debugger, driven unattended over a pty, the memory reads, watchpoints and breakpoints it does and does not give, and `tools/dos/dosboxx.py`, which hides its four traps |
| [143-winuae-debugger.md](143-winuae-debugger.md) | The Amiga side's answer: WinUAE's console debugger driven from Linux over the Windows VM, why stock WinUAE has no GDB server, `S`-to-a-file as the read path, and the built-in trainer search as the way to find a live address |
| [144-decoding-a-new-title.md](144-decoding-a-new-title.md) | The method for a title nobody here has opened: which Pool of Radiance facts may be assumed and which must be re-measured, the order of attack cheapest first, finding live data at unknown addresses, the confidence discipline, and the twenty-step checklist |
| [145-dos-decode-kit.md](145-dos-decode-kit.md) | A stranger's MIT-licensed DOS reverse-engineering kit read against our tables: the ECL bytecode is the same bytes on both ports, plus the `88 13` header word, the GEO door slice, and the DOS byte it misreads as a class group |
| [146-unified-ui.md](146-unified-ui.md) | The plan for one `wish/window.ui` holding the whole application layout, so Qt Designer shows the window a user sees: what is inlined and pre-created at maximum capacity, what stays a separate dialog |
| [147-combat-rolls.md](147-combat-rolls.md) | What the game rolls when somebody attacks: the generator, the d20 at `$2B10` with 20 stored as 100, the number to beat at `$A4F0` and the damage at `$A4F8`, all readable by ordinary polling, plus the five readings that do not work |
| [148-d6502.md](148-d6502.md) | The `tools/c64/d6502.py` disassembler's opcode table checked against a hand-built regression suite (including the `$F6` entry, `INC $nn,X`) and capstone sweeps that prove nothing else disagrees |
| [149-driving-a-dos-fight.md](149-driving-a-dos-fight.md) | Answering an encounter and finishing a fight under DOSBox with no debugger: what `QUICK` does (one keypress per combatant), the nine screens a fight puts up and the digest each is known by, and the four assumptions that do not hold |
| [150-departing-prologues.md](150-departing-prologues.md) | What a fast travel skips when it enters `NEWECL` at `$2034`: all seventy-nine exits in the thirty area scripts, each prologue's writes graded against what `newecl_writes` sets; three entries need repair and `SAVE 255, [$6DC9]` costs nothing |
| [151-quest-flags.md](151-quest-flags.md) | **Generated** by `tools/areas/eclflags.py doc`: every reference the thirty area scripts make to `$4A00`-`$4AF8`, the page the game records what the party has done on: which script writes each byte, what values, how many instructions read it, and whether a printed speech sits in the block that writes it |
| [152-commodore-manuals.md](152-commodore-manuals.md) | The three original Commodore manuals kept on this machine (User's Guide, Programmer's Reference Guide, 1541 User's Guide, none in the repository): edition off each title page, which has a text layer, what each is the authority for, the PDF-to-printed page offsets, and the one place a path to them is written down |
| [160-why-these-rules.md](160-why-these-rules.md) | The history behind this project's working rules, one heading per rule file: what was done, what it cost and Donald's own words where they settled it; read it when a rule looks arbitrary |
| [161-c64-ultimate.md](161-c64-ultimate.md) | The C64 Ultimate and its `c64u` CLI (`tools/c64/c64u.py`) as a second, independent reading of the machine, and the four hardware behaviours that decide how a dump off it is read (DMA reads whatever banking the CPU has at the time, `$00`/`$01` read the RAM under the processor port, VIC colour registers read back with unused top bits set, a DMA write persists only where nothing else drives the address), with the unknowns listed beside the experiment that would settle each |
| [162-spc-permanence.md](162-spc-permanence.md) | The DOS engine decides a `.SPC` effect record is permanent from the sixteen-bit duration at bytes 1-2 and nothing else; `add_affect`'s five arguments, every call site tabulated by what it writes, and the provenance test that shows an effect id outside the twelve the engine writes permanently was put there by an editor |
| [163-dos-vm-address-map.md](163-dos-vm-address-map.md) | The DOS saved game's 5120-byte variable array is the ECL VM's memory at its own addresses (`$4900`-`$4CFF`, `$6B00`-`$6EFF`, `$9700`-`$98FF`), which names every word `141-dos-savegame.md` could not: `$6DD2`/`$6DD3` are the rest-interruption interval and chance, `$6E7A`-`$6E7C` the overland script's loop registers, file bytes 12804-12805 the wall ahead and the square's attribute |
| [164-ssb-spell-slot-block.md](164-ssb-spell-slot-block.md) | Silver Blades' 28-byte spell-slot block is `array[0..3, 1..7]` indexed by the spell table's class byte; class 2 is vacant in this title, so `0x140`-`0x146` is zeroed by the engine and never added to, compared with the same block on Curse, Gateway, Pools of Darkness and Treasures |
| [165-amiga-savegame.md](165-amiga-savegame.md) | The Amiga saved game's file structure: a straight sequence of writes, with the 5120-byte variable array split across three heap pointers (the regions `163-dos-vm-address-map.md` names for DOS), the party count as the only table of contents, each title's square-region layout, the game-mode enumeration, and that the two later titles load and resave a save this project wrote |
| [166-amiga-records-from-the-code.md](166-amiga-records-from-the-code.md) | The Amiga Curse and Silver Blades character and item records, read out of the routines that unpack them from the DOS layout: Curse's druid slot array at `0x134`, the two Curse pads beside `charges`, the 70-byte Silver Blades item node, where `sex` and `alignment` sit, and the defect in Curse's monster loader that misplaces two slot arrays |
| [167-amiga-neutral-and-party-writing.md](167-amiga-neutral-and-party-writing.md) | Reading an Amiga Curse or Silver Blades record into the neutral record and writing one back: the status word and `active` flag, and the write rule that the item- and effect-chain head pointers are booleans the loader tests and overwrites, so a wrong one desynchronises every character after it |
| [168-dos-dax-and-combat-icons.md](168-dos-dax-and-combat-icons.md) | What `CHEAD.DAX` and `CBODY.DAX` hold and how DOS composes a combat figure from a record's four icon fields: the image block format, the part-to-pixel-value table, the pose composition, the six-part recolour lookup, the numbering that holds across all three DOS titles, and Silver Blades' redrawn blocks with the per-title, per-size override they need |
| [169-dos-combat-side.md](169-dos-combat-side.md) | DOS character-record `0x10E` is the combat side (0 the party's, 1 the enemy's): the per-side count, victory test, target picker, the `Attack Ally:` betrayal rule, Animate Dead and the party-panel colour, plus the engine's script-field accessor that maps C64 record `0x10C` (`0x81` hostile, `0x80` quickfight, `0`) onto DOS `0x10E` and `0x10F` |
| [170-c64-identity-pair.md](170-c64-identity-pair.md) | The C64 side of the DOS identity byte: record `0x0E6`-`0x0E7` is drawn from the generator at creation and read by nothing, the add screen refuses a duplicate by name alone, and `0x0E3` is the strength-adjustment flag |
| [171-c64-trait-slots.md](171-c64-trait-slots.md) | A C64 trait slot is one byte of persisted state meaning "has effect N", with no owner, duration or provenance, applied wherever `LIBRARY $4027` is asked (twenty combat check lists, the surprise check, any script); READY writes the id and nothing else, load never re-derives a slot from a readied item, and the C64 writer of an item-granted effect has to write the id in a free slot and `+14`/`+15` on the item |
| [172-curse-trainer.md](172-curse-trainer.md) | Curse of the Azure Bonds' trainer, measured: the hall gate `$7EA8` and the menu mask at `GEN $12AF`, six differences from Pool of Radiance (one press raises every ready class at 1000 gp each, `UNABLE TO ADVANCE`, a refused training costs nothing, the paladin's turning level, `GEN $2515` handing out traits 45 and 134, no spell capacity written), and 75 derived fields reproduced from `goldbox/levels.py` and `goldbox/levelup.py` |
| [173-carrying-limits.md](173-carrying-limits.md) | How many items one character can hold: sixteen on every port and title, enforced by one routine in all eight DOS and Amiga executables that shares its refusal flag with the weight test (so the player sees only `Overloaded`), acquisition only, with the per-title offsets and the two commands that disappear at sixteen |
| [174-combat-figures-in-the-running-game.md](174-combat-figures-in-the-running-game.md) | How a party's combat figures are read off the running C64 and compared against the eighteen screen codes a save holds: the engine renumbers each combatant into its own nine codes in a combat character set at `$D000` (RAM under I/O), copied from `CHARPIC00`, with the window geometry and the mirror rule for a figure facing the other way |
| [175-silver-blades-save-conversion.md](175-silver-blades-save-conversion.md) | What a DOS-to-C64 Secret of the Silver Blades conversion writes and where the ports disagree: Curse's container under the name `SAVEDBASH`, three header rows changing hands, a quest-flag page five bytes longer, and the four faults the running game exposed (lower-case names, ranger read as paladin, the 67-byte item, five zeroed flag bytes) |
| [176-changing-class-twice.md](176-changing-class-twice.md) | A Gold Box character cannot change class more than once, in all four ports asked, and each refuses by reading the field the first change wrote (the C64's `LDA $7CBA / BNE` at Curse `GEN $2393` and Silver Blades `GEN $1F88`, the DOS menu line dropped or refused in the command), with the census of 62 DOS records showing none holds two |
| [177-a-load-that-goes-wrong.md](177-a-load-that-goes-wrong.md) | What was measured when Pool of Radiance stops on a C64 Ultimate: the opening demo reproduces it, the test that tells a hang from a load, the `$DD00` readings idle, loading and hung, and that four candidate causes stay live because one machine cannot separate a firmware defect from a fault in that machine |
| [178-turning-undead.md](178-turning-undead.md) | Where each port keeps a cleric's power to turn undead: the C64 gates the word `TURN` on record `0x0A4` (`COMBAT $09D9 LDA $6BA4 / BNE`), DOS keeps no such byte and bands the cleric level out of `class_levels[0]` when Turn is pressed, so DOS `0x076` is the C64's `turn_class` and not its `turn_power`; also Silver Blades' routine at `GEN $13A5` |
| [179-loading-a-curse-save.md](179-loading-a-curse-save.md) | What Curse's `LOAD SAVED GAME` does and the three faults that make it print `UNABLE TO LOAD SAVED GAME.` in a pooled session (a second Return left in the KERNAL buffer, waiting inside a monitor connection so the drive never settled, and an unclosed `*PRG` that the drive refuses to open), the once-per-boot prompt gate at `GEN $182D`/`$03B4`, and the sequence that gets a party in |
| [180-writing-a-later-dos-record.md](180-writing-a-later-dos-record.md) | What `goldbox.dos_codec.write` needs to build Curse's 422-byte and Silver Blades' 439-byte DOS records: the per-title width table (ability pairs, spell books, class-level slots, spell-slot levels, the 67-byte `.STF` item stride), the round-trip counts, `paladin_cures`, and that no DOS container is produced for those titles |
| [181-curse-picture-buffer.md](181-curse-picture-buffer.md) | The 1024 bytes at `+$1800` of a Curse `SAVEAZURE` are `ANIMATE00`'s picture buffer at `$6300` (the decoded glyphs and colours of the picture in the view window at whatever animation frame it had reached), not map memory, with the decoder at `$6931`-`$6B09`, the `PIC` format it implies, and that `CAMP` uses the roster page as scratch and rebuilds it before the save |
| [182-amiga-por-in-the-running-game.md](182-amiga-por-in-the-running-game.md) | What Amiga Pool of Radiance does with character files this project wrote: the ITEMS screen composes an item's row from `name1`, `readied` and the rest even when the node's 42-byte display line is entirely NUL and writes the render back into it, a character owning nothing is offered no ITEMS entry, and the engine's own re-save differs only in live pointers and derived fields |
| [183-the-two-rings-of-fire-resistance.md](183-the-two-rings-of-fire-resistance.md) | Pool of Radiance's C64 disks carry five Ring of Fire Resistance records and four grant effect 61 (`ITEMFILE1D` on POOL4, which DOS `ITEM4.DAX` block 29 matches byte for byte, and three readied on monsters); the fifth, in `ITEMFILE17` on POOL3, has `+14` and `+15` zeroed, and `load_item_templates` kept it because POOL3 sorts first |
| [185-a-party-that-has-not-set-out.md](185-a-party-that-has-not-set-out.md) | Why a Curse save can hold area 0 when Curse has no area 0: forming a party and saving before `BEGIN ADVENTURING` writes it, 0 is the initialiser's value and not a place, and a C64 save that names area 0 sends the loader after `GEO00` with side 2 in the drive, so converting the word unchanged makes a save the player cannot start |
| [186-ready-and-action.md](186-ready-and-action.md) | Where a combat icon's second nine screen codes go: the icon editor labels the two poses `READY` and `ACTION`, and `COM.PREP $122C` expands all eighteen codes of all eight save slots into a table at `$9BE8` (18 glyphs of 8 bytes then 18 colour bytes per slot) from `CHARPIC00` staged at `$8C00`, with where each pose is proven and how long ACTION is on screen left open |
| [187-the-class-code-byte.md](187-the-class-code-byte.md) | The class code (`char_class`, C64 `0x073`) against the class bitmask, and that Curse's C64 `GEN $1939` stores `A` instead of `X`, so every character Curse's own trainer touches reads CLERIC; the per-title code table (index 10 differs between titles) and the repair `goldbox.dos_codec.write` makes from the record's class mask, and from the level array only for a dual-classed character |
| [188-the-sheet-portrait-per-title.md](188-the-sheet-portrait-per-title.md) | Only Pool of Radiance draws a sheet portrait: `LIBRARY $48A4` reads the record's two portrait bytes, loads the `HEAD`/`BODY` files they name and draws them at `$CC44`, and neither later title calls it, so a converted party loses nothing by arriving without a face |
| [189-effect-97-from-the-code.md](189-effect-97-from-the-code.md) | DOS Pool of Radiance's innate effect 97 is written at creation on the race byte alone (`0x1A12A`) and its handler (`0x112E5`) reads the character's constitution when a saving throw is rolled, adding `constitution * 2 // 7` on the wands and spell columns, with 90 doing the same on the paralysis/poison/death column, so writing 97 from a race table cannot hand out a bonus nobody rolled |
| [191-the-amiga-save-disk.md](191-the-amiga-save-disk.md) | The disk a player is handed when a party is converted to Amiga Pool of Radiance: an 880K floppy named `POOLSAVE` with the party on it and no game code, answered for at `PATH FOR SAVE  RETURN = POOLSAVE:` with a bare RETURN, since the prompt's default is a volume name and the slot files sit in the volume's root; the engine loads and resaves it, and what is not established is a one-drive Amiga, `charlist.txt`, and any title but Pool of Radiance |
| [192-curse-dual-class.md](192-curse-dual-class.md) | Curse's trainer for a character who has dual-classed, and the round-up rule in its hit-point divide, measured over eight driven trainings |
| [197-duplicating-the-c64u-hang.md](197-duplicating-the-c64u-hang.md) | How to hang a C64 Ultimate on purpose with the machine, its shipped USB stick and `curl`: a three-line BASIC read loop plus a `readmem` loop from the host, the jiffy clock as the only liveness reading, the four traps that give wrong verdicts, the two resting values of `$DD00` a bus check has to know about, and the workaround as built |
| [198-upstream-report-readmem-idle-bus.md](198-upstream-report-readmem-idle-bus.md) | Draft, not sent: the report for the 1541ultimate project that `machine:readmem` halts the 6510 and a halt inside a serial-bus transfer leaves the C64 in the KERNAL's `ACPTR` wait with interrupts off, with the frozen state, the halt cost, the size table and the ask |
| [193-a-dos-figure-on-the-c64.md](193-a-dos-figure-on-the-c64.md) | How a DOS character's combat figure becomes a C64 one: what each side stores (a body, a head, a size and six EGA colour pairs against eighteen `CHARPIC00` screen codes and colour nibbles), why the mapping is a judgement and not a derivable table, the 46 chosen rows, and the nine rows a small character has to wear from the large list |
| [194-the-dos-training-ladder.md](194-the-dos-training-ladder.md) | How a DOS Pool of Radiance party is levelled through the game's four training schools without playing for hours: the hall's square-to-school map, the party-menu quirks that make a driving script fail silently, the word at `0xD51` as a school's class-bit filter, the trainer's experience-clamp rule, and that DOS ships a different THAC0 table outright; `tools/dos/dosladder.py` is the tool |
| [195-three-dos-record-bytes-named-from-the-overlays.md](195-three-dos-record-bytes-named-from-the-overlays.md) | Four DOS record bytes named from the constants the overlays put around them: `0x0BF`/`0x143` are `combat_figure`, not marching order, `0x10D`/`0x196` are the `in_combat` byte, `0x084`/`0x0F7` are coab's `Control` enum with a morale percentage in the low seven bits, and `0x085`'s treasure-share bit records `MODIFY CHARACTER > KEEP` |
| [196-the-amiga-saved-game-built.md](196-the-amiga-saved-game-built.md) | Building the Amiga Pool of Radiance saved game from a source party: the Amiga container mapped field for field against DOS's, the DOS map holding at the same addresses, the area's ECL script read off disk 2's single `ecl.dax` (all 843 blocks unpack with a zero checksum), and the outdoor view-type and wall-in-front bytes; `tools/amiga/toamigapor.py` is the writer |
| [199-amiga-combat-icons.md](199-amiga-combat-icons.md) | An Amiga Curse or Silver Blades record's combat-icon fields mean what DOS's mean, read out of the routine that draws the figure: the same offsets, the ICON menu wrapping at 13 and 31, the same six recoloured parts, `CHEAD.TLB`/`CBODY.TLB` holding DOS's block ids, and the one difference, a plume highlight drawn in palette entry 0 |
| [200-innate-effect-seeding.md](200-innate-effect-seeding.md) | Where a DOS innate effect id comes from, read out of the engine's creation switches on race and class for all three titles (Curse seeds the elf 107 and the ranger 134, Silver Blades the ranger 105, Pool of Radiance no class effect), and that the DOS engine neither re-seeds nor strips an innate effect on load and save where the C64's `GEN $2515` does |
| [201-the-two-ability-arrays.md](201-the-two-ability-arrays.md) | The second ability array at `0x065` in the later C64 titles is the permanent score and `0x014` is what is in force, derived from it by the recompute at `ECL65 $913B` that copies base to current and adds drains, items and spells; the sheet, carrying allowance, combat and spells read `0x014`, generation and the training hall's level cap read `0x065`, and the racial level-limit routine subtracts a penalty rather than a bonus |
| [202-the-amiga-effect-node-pad.md](202-the-amiga-effect-node-pad.md) | The byte at offset 1 of an Amiga Curse or Silver Blades effect node is the alignment pad in front of the duration word and holds nothing: no instruction in either executable writes or reads it, argued by counting every place a node pointer can be born and every consumer downstream, and confirmed by a converted party that loaded with zero there and was written back with it intact |
| [203-a-converted-later-amiga-party-in-the-running-game.md](203-a-converted-later-amiga-party-in-the-running-game.md) | What Amiga Curse and Silver Blades did with a party `goldbox.amiga_later.write_later` converted off a C64 save: both loaded it, drew the sheets and items and wrote the party back at the same length, with the resave diff masked by the writers' own declared lists, and that a 70-byte and a 66-byte item node can be written with 42 NUL bytes where the display line goes |
| [204-the-dos-ability-pair.md](204-the-dos-ability-pair.md) | Which byte of a DOS ability pair is the score in force differs by pair: for strength through charisma the lower byte is the permanent score and the higher is in force, for the exceptional-strength percentile `0x01C` is in force and `0x01D` permanent, read out of five shipped engines and measured in DOSBox with the damage bonus as the engine's own arithmetic |
| [205-the-c64-thac0-rebuild.md](205-the-c64-thac0-rebuild.md) | The C64 engine does not read a character's stored `thac0_current` in a fight: `LIBRARY $3918` in Curse and `$3729` in Pool of Radiance rebuild the roster block from `thac0_base` and the AD&D strength tables when the fight starts, gated by `0x0E3`, so the sheet can show a THAC0 up to three points worse than the one he fights with until his next fight |
| [207-c64-rest-interruption.md](207-c64-rest-interruption.md) | What interrupts a rest on the C64: `CAMP $1E0F` skips the check when `$6DD2` is zero, each area script decides whether a rest can be interrupted at all (six of the thirty never, two always), and the Slums is checked only with the murder penalty `$4A0B` set; also the DOS two-byte difference and the census of all thirty scripts |
| [208-the-class-combo-and-the-conversion.md](208-the-class-combo-and-the-conversion.md) | The editor's Class combo (class bitmask) and the conversion (level array) cannot disagree on a Curse character on the C64, because every one of the five sites on the six Curse sides that stores either field derives one from the other, writes both under one test (`GEN $20A3`), or leaves the set of classes alone; the regain rule is CONFIRMED |
| [206-three-amiga-questions.md](206-three-amiga-questions.md) | Three things a running Amiga game does: the Pool of Radiance character sheet draws no portrait and no box for one, the engine strips every space out of every name it saves, and Pools of Darkness lists a `.pc` this project wrote and a party takes it; with the driving notes each cost |
| [209-the-regained-dual-class-on-dos.md](209-the-regained-dual-class-on-dos.md) | What a DOS record holds once a dual-classed human regains his old class: no array moves, `class_levels[old]` stays zero and the recompute at Curse's `GAME.OVR:0x3B119` rebuilds `class_bits` from the former array (adding the old class's bit when its entry is non-zero and strictly less than `level`), and takes the regained class's THAC0 and turning level from it too; five of five titles carry the same routine |
| [211-what-the-game-says-when-a-monster-attacks.md](211-what-the-game-says-when-a-monster-attacks.md) | What the engine prints when a monster acts: one routine (`COMBAT $0D63`) prints every melee attack whoever swung, picking phrases out of `SPELLN00`'s single 64-message table, and the player's own `ENCAMP > SPEED` at 0 (`$49FC`) skips `COMBAT $28C3`'s delay and so silences most of the panel |
| [212-the-live-tab-per-title.md](212-the-live-tab-per-title.md) | The automapper tab measured against a running Curse and Silver Blades: ten checks a title through the code the window ships (`Automapper.poll()`, `live.read_snapshot`, `live.badges`, the five `automap.actions` buttons), the live cards against the same save read cold, and that a fast travel moves the block at `$0400` and `AutomapState.area` while the save image's own area byte does not change; `tools/gui/livecheck.py` re-takes it |
| [225-the-dos-backstab-multiplier.md](225-the-dos-backstab-multiplier.md) | The three later DOS titles and all six C64 titles compute backstab from the thief level rather than storing it; the per-title formulas, dual-class regain paths, instruction addresses and reproducing readers. |
| [226-the-c64-running-effect-crosswalk.md](226-the-c64-running-effect-crosswalk.md) | The three C64 duration packers, Pool's DOS/C64 effect ids and value encodings, and the remaining clock, ownership and overlapping-effect questions. |
| [227-editor-open-save-as.md](227-editor-open-save-as.md) | The split Open and Save controls, native and cross-platform Save As behavior, destination publication, and acceptance requirements for conversion without dropped fields. |
| [228-pools-of-darkness-spells-and-creation.md](228-pools-of-darkness-spells-and-creation.md) | Pools of Darkness' four spell-slot progressions to class level 29, its wisdom and intelligence gates, every spell id's class and level, its spellbook ceiling and its creation menus, read from both ports. |
| [229-the-npc-window-bytes.md](229-the-npc-window-bytes.md) | The three unnamed bytes of `field_83_87` read from every engine's code on all three platforms: the window is the C64's `0xB7`-`0xBA` with the share inserted, the fourth byte is the C64 `dual_class_slot` position and is read by Silver Blades' importer on DOS and the Amiga, the fifth is the `dual_class_level` position and the first the C64's own unnamed `0xB7`, neither read anywhere; the addressing forms searched, and the one import experiment left |
| [230-who-reads-a-dos-effect-node.md](230-who-reads-a-dos-effect-node.md) | Which effect ids' DOS nodes the engine reads past the duration, per title; Dispel Magic's `0xFF` exemption and `remove_affect`'s flag as the readers of every node; what a readied magical item writes in each title; Pool of Radiance's strength-item value byte; and why Silver Blades' C64 halfling 92 is not DOS's 97 |
| [231-where-lay-on-hands-lives.md](231-where-lay-on-hands-lives.md) | Where each port keeps a paladin's spent lay on hands -- C64 record `0x013` plus a one-day row, DOS and Amiga a one-day effect node and no byte -- with the effect id per title and port (Amiga Silver Blades and Pools of Darkness use 140 where DOS uses 109), the read and write rules a converter needs between every pair of ports, and what stays open |
| [232-the-c64-control-byte-per-title.md](232-the-c64-control-byte-per-title.md) | Record byte `0x0B8` read out of all six C64 titles' code: bit 7 is control everywhere, the low seven bits a morale doubled and clamped at 100 except in Pool of Radiance, the trainer flag exists only in Pool of Radiance, what each join, berserk, cure and import path does to the byte, and which control, morale and ability-altered fields the editor can show per title and port |
| [233-herdr-advisor-and-orchestrator.md](233-herdr-advisor-and-orchestrator.md) | How the advisor and senior advisor identify sessions and relay authorized instructions to the orchestrator through Herdr, with connection evidence and access requirements. |
| [234-a-paladins-cure-disease-across-dos-and-the-c64.md](234-a-paladins-cure-disease-across-dos-and-the-c64.md) | What a C64 writer must put in record `0x012` and the effect arrays for a converted DOS Curse or Silver Blades paladin's cure-disease uses and node, case by case, with what the running C64 game did with each, the DOS rule read from `GAME.OVR`, and the one DOS state C64 Curse cannot hold |
| [architecture.md](architecture.md) | A high-level sketch of the application (the editor/automapper split, the VICE binary-monitor connection, a tour of `wish/`, `automap/`, `editor/` and `goldbox/`), stale against the current code: it gives the record as 151 bytes where `RECORD_SIZE` is 580, and names classes `CharacterEditor` and `AutomapWindow` where the code has `EditorBinding` and `AutomapBinding` |
| [ui-architecture.md](ui-architecture.md) | The Qt Designer pattern the UI is built on (one `.ui` file compiled by `pyuic6`, promoted custom widgets, Python controllers that find widgets by `objectName`), correct in general and stale in its inventory: it lists standalone `editor/spellbook.ui` and `editor/memorised.ui` and a `CharacterEditor` class that no longer exist |
