#!/usr/bin/env python3
"""A retired entry point that checks nothing and exits 0, kept so a session with cached hook wiring does not fail on every command.

Neither `.claude/settings.json` nor `.codex/hooks.json` registers it.
"""
