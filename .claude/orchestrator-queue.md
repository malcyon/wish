# Orchestrator queue

Owned by the `/orchestrate` skill (`.claude/skills/orchestrate/SKILL.md`), which has the rules for reading and rewriting this file. This file is only the table and the two lists.

| order | issue | subagents, in order | what to do, and what it waits on | status |
| ----- | ----- | -------------------- | --------------------------------- | ------ |
| 1 | #569 (tools/laterbattle.py --title ssb crashes on the first script bar a Silver Blades walk meets (Session/SSBSession have no press_bar)) | junior-dev | Found while re-validating #334. `cursethac0.Run.clear_bar`, used by `tools/laterbattle.py`'s own official `--title ssb` combat-walk path, calls `self.sess.press_bar(...)`, which exists only on Curse's session class. Blocks the official combat-walk tool for any future Silver Blades work (#89). Fix is either switching to `select_bar` (exists on every session type) or adding an equivalent `press_bar` to `SSBSession`, whichever investigation shows is right. | junior-dev running now |
| 2 | #563 (Secret of the Silver Blades can draw condition badges again, now that sixteen of the seventeen badged codes are named) | qt-ui-specialist to draw, then stop; junior-dev after Donald's yes | Step 1 of the body is a decision about how the roster card looks. Draw one Silver Blades roster card with tools/rostercard.py using Pool of Radiance's seven badge groups minus 35 and 38, offscreen, post it on the issue, and stop: Donald looks at it. Nothing else on this issue until he answers. Steps 2 to 4 are named in the body and are junior-dev's once he has. | waiting on Donald — card posted to the issue, `BADGE_TABLES` edit reverted |
| 3 | #15 (Fast Travel for more than one Gold Box title) | reverse-engineering to propose, then stop; emulator-runner and junior-dev after Donald's yes | Curse is done, 24 of 25 named. The forum thread docs/126 cites for Silver Blades holds no area table, so the names come from the game itself: read each of the 22 Silver Blades areas' own script strings, the narration the game prints on arrival, and post a table of proposed names with the string each rests on. Stop there: Donald approves the list. Then the same validation run and landing that Curse had, by the 2026-09-15 comment's grading rule. | waiting on Donald — 20 of 22 names proposed and posted (`$04`, `$11` left unnamed on purpose); the dungeon's own level order (`$40`/`$41`) is also unsettled and named as a follow-up if he wants it numbered |
| 4 | #445 (The game's third fight outcome, THE PARTY RUNS AWAY, has never been seen on a screen) | emulator-runner, then close | The static read is done: `ECL00` writes 1 to `$6DE6` once, unconditionally, right before New Phlan's tavern brawl. 2 of 4 remaining sub-questions landed clean (`34a3004`): BAR_CANCEL CONFIRMED reads as NO, and the `$1544` mercy-heal PROBABLE from an earlier comment is now REFUTED with the harness's own wound patch removed from the experiment. 2 still open: a Curse flee has no outdoor/dungeon Curse save to test from; `$6DE3`'s award-tally test needs new driving capability. | review found a stale-log warning and a banned word; also flagged that the REFUTED grade rests on an unverified assumption about `POST.COM`'s timing. reverse-engineering fixing both and checking the timing now |
| 5 | #516 (Generate boundary characters and check every writer's field widths, since no real save reaches a limit and the corpus cannot find a wrong one) | junior-dev for slices 1-2 (done), then reverse-engineering for slice 3 | Slices 1-2 committed (`09392c7`, `efd63b3`), reviewed clean on the second pass. Slice 3 (the engine-check stage) only ever asserted `spells_memorised`; three uncorroborated rows (`spells_castable_cleric`, `attack_forms`, `field_83_87`) need a `--sites` disassembly read instead of the family scan's loop-bound guess. A fifth round-trip exclusion (`attack_level`, tied to `#527`) was found and reported, not silently fixed. | slice 3 running now |
| 6 | #10 (Finish the high-level test party) | emulator-runner | One fully specified session in the thread settles its four remaining sub-questions and closes the ticket. | emulator-runner running now |
| 7 | #446 (The Amiga saved game's zero argument rests on three of the game's twenty-nine areas) | emulator-runner (done), then junior-dev | Two new Amiga saves (Temple of Bane, Valjevo Castle the Pool) both came back clean — no new non-zero word among the 22 the ticket was measuring, which is stronger evidence for the writer's zero claim than "unvisited" was. Landing `POR_SAVGAM_UNVISITED` in `goldbox/amiga_savegame.py`, updated for the two new specimens, is what's left. | junior-dev running now (bundled with #571) |
| 8 | #570 (Three drivers (livecheck.py, inventorycheck.py, cursecheck.py) still call cursewarp.enter_world with no Addresses, so they still carry #568's Escape-abort vulnerability) | junior-dev | Filed from #568's own closing comment. Each needs to build a `cursewarp.Addresses` and pass it through, the same plumbing `#568`'s fix required of `enter_world` itself. | queued |
| 9 | #571 (tools/amigazerowords.py crashes on every run: a stale attribute reference from #534's consolidation) | junior-dev | One-line fix: `tools/amigazerowords.py:149` reads `amiga_por.POR_SAVEGAME_SIZE`, which moved to `goldbox.amiga_savegame.POR_SAVEGAME_SIZE` in `#534`. Found while working #446. | junior-dev running now (bundled with #446's table) |
| 10 | #89 (Silver Blades' trainer grants spells from a table, and goldbox/levelup.py offers them from a menu) | senior-analyst, then reverse-engineering | The ticket's premise was corrected mid-thread. The analyst reads the correction and goldbox/levelup.py and names what remains: a driven Silver Blades training diff with tools/ssbtrain.py so TRAINER_MEASURED stops excluding that title, and the spell-slot capacity tables read from the binary. Now unblocked by #334's combat fix and #569's own fix (in progress). | queued |
| 11 | #555 (The DOS driving harness cannot select a spell to memorize or choose which party member a screen acts on) | senior-analyst, then junior-dev | The body says nothing has been designed. The analyst reads tools/dosbox.py and the screens the body names and posts the mechanism. | queued |
| 12 | #449 (The journal reader's rule-book half has never been read off a screen, so 20 of its 50 challenges are untested live) | emulator-runner | The repeat-boot capture experiment the thread specifies. | queued |
| 13 | #500 (What unit is an active effect's duration in, and what do bits 6-7 of the duration byte select?) and #501 (Which active effect ids read their magnitude back when the effect expires, and which discard it?) | reverse-engineering | One agent for both: the same expiry routine answers both and one emulator session covers the two measurements. | queued |
| 14 | #560 (Does Pool of Radiance grant thief abilities off class_bits, or off the level array and skill bytes?) | reverse-engineering | Reachable only on a hand-edited record. The body names two routes; take the disassembly read of the backstab multiplier and thief-command gate, the cheaper one, and answer the question. | queued |
| 15 | #207 (Run an exit's own handler before Fast Travel warps out) | emulator-runner; then architect, then junior-dev | An independent corroboration run first, since the only sample is the author's own. The two-hop case needs a wait-for-idle mechanism across automap/actions.py and automap/window.py that does not exist, so the architect plans it before anything is built. | queued |
| 16 | #249 (Build a DOS party from creation and level it ourselves, so DOS measurements rest on records we watched being written) | junior-dev | Donald ruled on 2026-09-15 that the specimen backup repeats weekly; the comment of that date says what to build. A weekly job runs tools/specimenbackup.py archive into /data/OneDrive/wish-specimens/ with a dated name, then verify. Copy the mechanism that takes the hourly snapshot of work/; if it cannot be found, a systemd user timer, committed under tools/ with a README row. Closes after one unattended firing passes verify. | queued |
| 17 | #323 (The encumbrance identity does not survive the training fee, so failing it is not evidence of an edited record) | reverse-engineering | Sweep Curse's item tables for a bag-of-holding record, the one loose end, then close. | queued |
| 18 | #566 (Session.save_game trusts select_bar's truthy return with no check that the real bar it wanted actually appeared) | junior-dev | Priority: Low, found in review of #565, not reproduced live. The body names the fix: mirror the verify-by-effect pattern `curserun.py`'s `enter_move()` already uses on `select_bar`, applied to `save_game`'s three `ENCAMP`/`SAVE`/`SAVE GAME` steps in `tools/session.py`. Write a test that forces an unrelated one-option bar to appear at one of the three steps and shows `save_game` wrongly accepting it today, then fix. Waits on nothing. | queued |
| 19 | #567 (Twelve of Curse of the Azure Bonds' own effect codes have no name at all, only a refusal of Pool of Radiance's wrong one) | reverse-engineering | Filed from #561's own closing findings. `tools/traitquery.py curse-of-the-azure-bonds --handlers` is the route; the anchors table `docs/171-c64-trait-slots.md` was built for Silver Blades and needs re-deriving against Curse's own addresses first, the way `#497` did for Silver Blades' last 35 codes. Priority: Low, question, no consequence attached yet. | queued |

## Closed, do not touch

#52, #359, #380, #454, #256, #552, #551, #533, #39, #510, #553, #557, #558, #559, #562, #497, #554, #556, #564, #565, #21, #561, #334, #568.

## Do not schedule

#194, #462, #475 (Pools of Darkness), #375 (blocked on the Ultimate), #477, #511, #464, #461, #37, #11, #488 (paused at Donald's instruction on 2026-09-10 pending a different diagramming tool than pyreverse).
