# Orchestrator queue

Owned by the `/orchestrate` skill (`.claude/skills/orchestrate/SKILL.md`), which has the rules for reading and rewriting this file. This file is only the table and the two lists.

| order | issue | subagents, in order | what to do, and what it waits on | status |
| ----- | ----- | -------------------- | --------------------------------- | ------ |
| 1 | #334 (The session driver cannot fight in Curse or Silver Blades, and says the party is not in a fight while it is standing on the combat floor) | junior-dev to fix, then a live re-validation | Root cause found: `tools/ssbwarp.py`'s `enter_world` sends Escape (RUN/STOP in VICE) at a screen unchanged for 15s, which aborts a mid-load `ECL` script — Silver Blades' scripts are 26-31 blocks and legitimately take longer than that to load. A partial load's missing tail reads as `$00` (EXIT), so a scripted fight silently never starts. Two earlier comments' readings on this thread were retracted against their own preserved evidence in the same report (the `$9AF9` byte, the "period-8 pattern" — actually VICE's power-on RAM fill). Same defect, independently, in `tools/cursewarp.py` — see `#568`. | junior-dev fixing both files now; once landed, a live re-run of the original Silver Blades combat experiment (walk to 15,12 and fight) settles whether the ticket is actually closed |
| 2 | #568 (cursewarp.py and ssbwarp.py can abort a mid-load ECL script by sending Escape to a screen that is merely slow, not stuck) | junior-dev (same agent as #334, one fix for both files) | The general form of #334's root cause: any driven session using either warp tool's `enter_world` can silently corrupt a load and report a false clean result. `ssbwarp.py` already has the guard (`idle_in_key_window`) and just doesn't call it in this branch; `cursewarp.py` has the matching machinery (`wait_idle`, `addr.key_wait`/`key_fetch`) and needs the same wiring. Open question the fix doesn't answer: whether any already-closed Curse finding rests on a run this hit — not chased, noted in the issue. | junior-dev fixing now, alongside #334 |
| 3 | #563 (Secret of the Silver Blades can draw condition badges again, now that sixteen of the seventeen badged codes are named) | qt-ui-specialist to draw, then stop; junior-dev after Donald's yes | Step 1 of the body is a decision about how the roster card looks. Draw one Silver Blades roster card with tools/rostercard.py using Pool of Radiance's seven badge groups minus 35 and 38, offscreen, post it on the issue, and stop: Donald looks at it. Nothing else on this issue until he answers. Steps 2 to 4 are named in the body and are junior-dev's once he has. | waiting on Donald — card posted to the issue, `BADGE_TABLES` edit reverted |
| 4 | #15 (Fast Travel for more than one Gold Box title) | reverse-engineering to propose, then stop; emulator-runner and junior-dev after Donald's yes | Curse is done, 24 of 25 named. The forum thread docs/126 cites for Silver Blades holds no area table, so the names come from the game itself: read each of the 22 Silver Blades areas' own script strings, the narration the game prints on arrival, and post a table of proposed names with the string each rests on. Stop there: Donald approves the list. Then the same validation run and landing that Curse had, by the 2026-09-15 comment's grading rule. | waiting on Donald — 20 of 22 names proposed and posted (`$04`, `$11` left unnamed on purpose); the dungeon's own level order (`$40`/`$41`) is also unsettled and named as a follow-up if he wants it numbered |
| 5 | #445 (The game's third fight outcome, THE PARTY RUNS AWAY, has never been seen on a screen) | emulator-runner, then close | The static read is done: `ECL00` writes 1 to `$6DE6` once, unconditionally, right before New Phlan's tavern brawl (`ECL00+$0E6A`) — the mercy behaviour is reachable in ordinary play, not an edited-save-only case. Two bonus findings from the same read: `POST.COM $1544` gives an unconscious character 1 hp back when `$6DE6` is set (a third gate, beyond the two the ticket already knew), and `$6DE3` (set by the same block) is probably what suppresses the brawl's award tally. Remaining live work: a driven Curse flee to confirm the wording on screen, BAR_CANCEL at a live combat bar, and the two new experiments the read proposed (stage `$6DE6` with no wound patch to confirm the `$1544` heal; win the brawl with `$6DE3` at 0 vs. 1 to confirm the tally suppression). Close with a comment when these land. | about to dispatch emulator-runner for the remaining live work |
| 6 | #516 (Generate boundary characters and check every writer's field widths, since no real save reaches a limit and the corpus cannot find a wrong one) | junior-dev for slices 1-2, then reverse-engineering for slice 3 | Plan posted: `tools/boundarychars.py` (new) and `tests/test_boundary.py` (new), hand-written cases (no `hypothesis` dependency — decided in the plan, nothing here needs Donald's yes), a `width()` lifted out of `tools/dosarraywidth.py`'s `report()`. Scoped to Pool of Radiance only; Curse and Silver Blades' own class ceilings are unread and go to reverse-engineering as a named follow-up. Slice 3 (the engine-check stage) only asserts `spells_memorised`, since `tools/dosarraywidth.py`'s other readings are a neighbouring loop's guard rather than a corroborated width — three uncorroborated rows go to a reverse-engineering `--sites` disassembly read instead of a builder's assertion. | about to dispatch junior-dev for slices 1-2 |
| 7 | #10 (Finish the high-level test party) | emulator-runner | One fully specified session in the thread settles its four remaining sub-questions and closes the ticket. | emulator-runner running now |
| 8 | #446 (The Amiga saved game's zero argument rests on three of the game's twenty-nine areas) | emulator-runner, then junior-dev | The two saves the thread names, Temple of Bane and Valjevo Castle, then the POR_SAVGAM_UNVISITED table it drafted. Closes when both land. | queued — next to dispatch |
| 9 | #89 (Silver Blades' trainer grants spells from a table, and goldbox/levelup.py offers them from a menu) | senior-analyst, then reverse-engineering | The ticket's premise was corrected mid-thread. The analyst reads the correction and goldbox/levelup.py and names what remains: a driven Silver Blades training diff with tools/ssbtrain.py so TRAINER_MEASURED stops excluding that title, and the spell-slot capacity tables read from the binary. | queued |
| 10 | #555 (The DOS driving harness cannot select a spell to memorize or choose which party member a screen acts on) | senior-analyst, then junior-dev | The body says nothing has been designed. The analyst reads tools/dosbox.py and the screens the body names and posts the mechanism. | queued |
| 11 | #449 (The journal reader's rule-book half has never been read off a screen, so 20 of its 50 challenges are untested live) | emulator-runner | The repeat-boot capture experiment the thread specifies. | queued |
| 12 | #500 (What unit is an active effect's duration in, and what do bits 6-7 of the duration byte select?) and #501 (Which active effect ids read their magnitude back when the effect expires, and which discard it?) | reverse-engineering | One agent for both: the same expiry routine answers both and one emulator session covers the two measurements. | queued |
| 13 | #560 (Does Pool of Radiance grant thief abilities off class_bits, or off the level array and skill bytes?) | reverse-engineering | Reachable only on a hand-edited record. The body names two routes; take the disassembly read of the backstab multiplier and thief-command gate, the cheaper one, and answer the question. | queued |
| 14 | #207 (Run an exit's own handler before Fast Travel warps out) | emulator-runner; then architect, then junior-dev | An independent corroboration run first, since the only sample is the author's own. The two-hop case needs a wait-for-idle mechanism across automap/actions.py and automap/window.py that does not exist, so the architect plans it before anything is built. | queued |
| 15 | #249 (Build a DOS party from creation and level it ourselves, so DOS measurements rest on records we watched being written) | junior-dev | Donald ruled on 2026-09-15 that the specimen backup repeats weekly; the comment of that date says what to build. A weekly job runs tools/specimenbackup.py archive into /data/OneDrive/wish-specimens/ with a dated name, then verify. Copy the mechanism that takes the hourly snapshot of work/; if it cannot be found, a systemd user timer, committed under tools/ with a README row. Closes after one unattended firing passes verify. | queued |
| 16 | #323 (The encumbrance identity does not survive the training fee, so failing it is not evidence of an edited record) | reverse-engineering | Sweep Curse's item tables for a bag-of-holding record, the one loose end, then close. | queued |
| 17 | #566 (Session.save_game trusts select_bar's truthy return with no check that the real bar it wanted actually appeared) | junior-dev | Priority: Low, found in review of #565, not reproduced live. The body names the fix: mirror the verify-by-effect pattern `curserun.py`'s `enter_move()` already uses on `select_bar`, applied to `save_game`'s three `ENCAMP`/`SAVE`/`SAVE GAME` steps in `tools/session.py`. Write a test that forces an unrelated one-option bar to appear at one of the three steps and shows `save_game` wrongly accepting it today, then fix. Waits on nothing. | queued |
| 18 | #567 (Twelve of Curse of the Azure Bonds' own effect codes have no name at all, only a refusal of Pool of Radiance's wrong one) | reverse-engineering | Filed from #561's own closing findings. `tools/traitquery.py curse-of-the-azure-bonds --handlers` is the route; the anchors table `docs/171-c64-trait-slots.md` was built for Silver Blades and needs re-deriving against Curse's own addresses first, the way `#497` did for Silver Blades' last 35 codes. Priority: Low, question, no consequence attached yet. | queued |

## Closed, do not touch

#52, #359, #380, #454, #256, #552, #551, #533, #39, #510, #553, #557, #558, #559, #562, #497, #554, #556, #564, #565, #21, #561.

## Do not schedule

#194, #462, #475 (Pools of Darkness), #375 (blocked on the Ultimate), #477, #511, #464, #461, #37, #11, #488 (paused at Donald's instruction on 2026-09-10 pending a different diagramming tool than pyreverse).
