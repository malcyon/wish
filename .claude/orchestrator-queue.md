# Orchestrator queue

Owned by the `/orchestrate` skill (`.claude/skills/orchestrate/SKILL.md`), which has the rules for reading and rewriting this file. This file is only the table and the two lists.

| order | issue | subagents, in order | what to do, and what it waits on | status |
| ----- | ----- | -------------------- | --------------------------------- | ------ |
| 1 | #563 (Secret of the Silver Blades can draw condition badges again, now that sixteen of the seventeen badged codes are named) | qt-ui-specialist to draw, then stop; junior-dev after Donald's yes | Step 1 of the body is a decision about how the roster card looks. Draw one Silver Blades roster card with tools/rostercard.py using Pool of Radiance's seven badge groups minus 35 and 38, offscreen, post it on the issue, and stop: Donald looks at it. Nothing else on this issue until he answers. Steps 2 to 4 are named in the body and are junior-dev's once he has. | waiting on Donald — card posted to the issue, `BADGE_TABLES` edit reverted |
| 2 | #15 (Fast Travel for more than one Gold Box title) | reverse-engineering to propose, then stop; emulator-runner and junior-dev after Donald's yes | Curse is done, 24 of 25 named. The forum thread docs/126 cites for Silver Blades holds no area table, so the names come from the game itself: read each of the 22 Silver Blades areas' own script strings, the narration the game prints on arrival, and post a table of proposed names with the string each rests on. Stop there: Donald approves the list. Then the same validation run and landing that Curse had, by the 2026-09-15 comment's grading rule. | waiting on Donald — 20 of 22 names proposed and posted (`$04`, `$11` left unnamed on purpose); the dungeon's own level order (`$40`/`$41`) is also unsettled and named as a follow-up if he wants it numbered |
| 3 | #445 (The game's third fight outcome, THE PARTY RUNS AWAY, has never been seen on a screen) | reverse-engineering, then emulator-runner, then close | The static read is done: `ECL00` writes 1 to `$6DE6` once, right before New Phlan's tavern brawl. BAR_CANCEL CONFIRMED reads as NO (`34a3004`). A prior review flagged the `$1544` mercy-heal grade as resting on an unverified timing assumption -- checked, corrected from REFUTED to UNTESTED (the losing arm's own menu blocks that code entirely). Two other sub-questions still open: a Curse flee has no outdoor/dungeon save to test from; `$6DE3`'s award-tally test needs new driving capability. | emulator-runner running now — `tools/fleedrive.py drive --no-wound --mercy`, the correctly-targeted experiment for the `$1544` question |
| 4 | #516 (Generate boundary characters and check every writer's field widths, since no real save reaches a limit and the corpus cannot find a wrong one) | junior-dev for slices 1-2 (done), reverse-engineering for slice 3 (done), junior-dev to promote the instrument (done), then reverse-engineering for steps 4-5 | Slices 1-2 committed (`09392c7`, `efd63b3`), reviewed clean. Slice 3 done: all three uncorroborated rows CONFIRMED as declared. `tools/dosrecordloops.py` (new, promoted) and `tools/dosarraywidth.py`'s two named defects fixed and committed (`4150224`, `3f7c3df`). Not close-ready: the plan's own steps 4-5 (widen the cases to the C64 writer; Curse/Silver Blades class legality) are still open and placed on this ticket. | reviewed clean, no findings; steps 4-5 not yet dispatched |
| 5 | #10 (Finish the high-level test party) | emulator-runner | One fully specified session in the thread settles its four remaining sub-questions and closes the ticket. | emulator-runner running now |
| 6 | #89 (Silver Blades' trainer grants spells from a table, and goldbox/levelup.py offers them from a menu) | senior-analyst (done), then reverse-engineering | Plan posted: two named blockers turned out not to be blockers (the level-up dialog already routes Silver Blades generically; a driven training already reproduces cleanly for one class). `tools/ssbtrain.py::compare` has a real bug (diffs a stale `--before` when a press changes the race byte between presses). What's genuinely untested is the spellbook itself -- twelve presses on `SILVER-6.D64` named, covering all four trainer classes, with the exact memory addresses to peek. The spell-slot capacity gap is unrelated and spun off as `#572 (Silver Blades' spell-slot progression tables have never been read, so a caster is shown no memorisation capacity)`. | reverse-engineering running now |
| 7 | #555 (The DOS driving harness cannot select a spell to memorize or choose which party member a screen acts on) | senior-analyst (done), then emulator-runner, then junior-dev | Plan posted: gap 2 (which character a screen acts on) needs no discovery -- `tools/dosportraitparty.py`'s `put_first` already does a byte swap in the party table that reaches any character with one conversion, it just hardcodes Pool of Radiance's offset and needs re-pointing per title. Gap 1 (moving the spell-list highlight) is named but unmeasured at that exact screen; the DOS engine's generic list-menu behaviour (`Home`/`End` move the highlight, `n`/`p` turn pages) is proven at other screens but never tried at this one. One bounded boot settles it. Also found: two #551 specimens carry a swapped character's hp/AC from a bad staging technique -- filed separately as #573. | emulator-runner running now (the one boot for gap 1) |
| 8 | #449 (The journal reader's rule-book half has never been read off a screen, so 20 of its 50 challenges are untested live) | emulator-runner | The repeat-boot capture experiment the thread specifies. | queued |
| 9 | #500 (What unit is an active effect's duration in, and what do bits 6-7 of the duration byte select?) and #501 (Which active effect ids read their magnitude back when the effect expires, and which discard it?) | reverse-engineering | One agent for both: the same expiry routine answers both and one emulator session covers the two measurements. | queued |
| 10 | #560 (Does Pool of Radiance grant thief abilities off class_bits, or off the level array and skill bytes?) | reverse-engineering | Reachable only on a hand-edited record. The body names two routes; take the disassembly read of the backstab multiplier and thief-command gate, the cheaper one, and answer the question. | queued |
| 11 | #207 (Run an exit's own handler before Fast Travel warps out) | emulator-runner; then architect, then junior-dev | An independent corroboration run first, since the only sample is the author's own. The two-hop case needs a wait-for-idle mechanism across automap/actions.py and automap/window.py that does not exist, so the architect plans it before anything is built. | queued |
| 12 | #249 (Build a DOS party from creation and level it ourselves, so DOS measurements rest on records we watched being written) | junior-dev | Donald ruled on 2026-09-15 that the specimen backup repeats weekly; the comment of that date says what to build. A weekly job runs tools/specimenbackup.py archive into /data/OneDrive/wish-specimens/ with a dated name, then verify. Copy the mechanism that takes the hourly snapshot of work/; if it cannot be found, a systemd user timer, committed under tools/ with a README row. Closes after one unattended firing passes verify. | queued |
| 13 | #323 (The encumbrance identity does not survive the training fee, so failing it is not evidence of an edited record) | reverse-engineering | Sweep Curse's item tables for a bag-of-holding record, the one loose end, then close. | queued |
| 14 | #566 (Session.save_game trusts select_bar's truthy return with no check that the real bar it wanted actually appeared) | junior-dev | Priority: Low, found in review of #565, not reproduced live. The body names the fix: mirror the verify-by-effect pattern `curserun.py`'s `enter_move()` already uses on `select_bar`, applied to `save_game`'s three `ENCAMP`/`SAVE`/`SAVE GAME` steps in `tools/session.py`. Write a test that forces an unrelated one-option bar to appear at one of the three steps and shows `save_game` wrongly accepting it today, then fix. Waits on nothing. | queued |
| 15 | #567 (Twelve of Curse of the Azure Bonds' own effect codes have no name at all, only a refusal of Pool of Radiance's wrong one) | reverse-engineering | Filed from #561's own closing findings. `tools/traitquery.py curse-of-the-azure-bonds --handlers` is the route; the anchors table `docs/171-c64-trait-slots.md` was built for Silver Blades and needs re-deriving against Curse's own addresses first, the way `#497` did for Silver Blades' last 35 codes. Priority: Low, question, no consequence attached yet. | queued |
| 16 | #572 (Silver Blades' spell-slot progression tables have never been read, so a caster is shown no memorisation capacity) | reverse-engineering | Filed from #89's own plan -- unrelated to that ticket's trainer question. `stores_spell_capacity=False` keeps this out of `plan`; the editor's memorised pane and the YAML export's capacity comment each have an explicit "not read" branch and show nothing rather than a wrong number, so no player sees a wrong answer. `tools/dosspellslots.py tables --game SECRET` does not answer it (`LookupError: 0 of 3 sites carry a class loop`) -- a different route is needed. Priority: Low, enhancement, no consequence attached yet. | queued |
| 17 | #573 (Two #551 DOS specimens carry a character's hp_current and armour class from a different character, because the reorder-before-convert staging swapped save slots without swapping the roster) | junior-dev | Filed from #555's own plan. Corpus hygiene, not a live defect -- nothing currently reads these two specimens and gets a wrong answer, and no player can produce this. Re-stage both with the byte-swap technique #555 lands, or annotate their provenance as known-corrupted, once #555's mechanism exists. Priority: Low. | queued -- waits on #555's mechanism landing first |

## Closed, do not touch

#52, #359, #380, #454, #256, #552, #551, #533, #39, #510, #553, #557, #558, #559, #562, #497, #554, #556, #564, #565, #21, #561, #334, #568, #569, #570, #446, #571.

## Do not schedule

#194, #462, #475 (Pools of Darkness), #375 (blocked on the Ultimate), #477, #511, #464, #461, #37, #11, #488 (paused at Donald's instruction on 2026-09-10 pending a different diagramming tool than pyreverse).
