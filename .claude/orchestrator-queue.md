# Orchestrator queue

Owned by the `/orchestrate` skill (`.claude/skills/orchestrate/SKILL.md`), which has the rules for reading and rewriting this file. This file is only the table and the two lists.

| order | issue | subagents, in order | what to do, and what it waits on | status |
| ----- | ----- | -------------------- | --------------------------------- | ------ |
| 1 | #556 (Saving a Curse or Silver Blades character disk rewrites every character's file to Pool of Radiance's load address) | junior-dev | A save that damages a player's disk. Root cause, file, line and test are all named in the body. Test seen red first. Waits on nothing. | closed — fixed, reviewed, `bd1925c` |
| 2 | #564 (Fast-travelling in Curse can show the arrival toast with Pool of Radiance's place name instead of Curse's own) | junior-dev | A wrong place name on screen. The body names the fix: thread the title through place_name, modelled on AutomapState.area_label. Waits on nothing. | closed — fixed, reviewed, `2d81c8a` |
| 3 | #565 (Session.select_bar cannot answer a one-option HORIZMENU, so a driven session times out at a script's own acknowledgement bar) | junior-dev | The body names tools/session.py:1036 and the sibling to copy. Every driven run in this queue that lands on a script page is waiting on this, so it goes early. | fixed, committed `344dd8b` — code review not yet run |
| 4 | #561 (A Curse of the Azure Bonds character's traits are named from Pool of Radiance's table, which disagrees with Curse's own data about eight codes) | reverse-engineering, then junior-dev | Wrong trait names in front of a Curse player. The body names two measurable pre-steps; take them, then land NAMES_CURSE in goldbox/traits.py with a grade beside each entry, the way #497 did for Silver Blades. A name is the game's own, so no wording question. | in progress — reverse-engineering measuring every Curse spell code and the monster census |
| 5 | #334 (The session driver cannot fight in Curse or Silver Blades, and says the party is not in a fight while it is standing on the combat floor) | emulator-runner, then reverse-engineering if the read names a cause outside memory layout | Curse is done and pushed. Silver Blades is one experiment away: the 2026-09-16 03:23 comment's section 6 specifies it, addresses and checkpoints included. Unblocks the Silver Blades halves of #445 and #89. Waits on item 3 if the run lands on a one-option bar. | in progress — emulator-runner driving a Silver Blades fight attempt (#565's fix landed, unblocking this) |
| 6 | #563 (Secret of the Silver Blades can draw condition badges again, now that sixteen of the seventeen badged codes are named) | qt-ui-specialist to draw, then stop; junior-dev after Donald's yes | Step 1 of the body is a decision about how the roster card looks. Draw one Silver Blades roster card with tools/rostercard.py using Pool of Radiance's seven badge groups minus 35 and 38, offscreen, post it on the issue, and stop: Donald looks at it. Nothing else on this issue until he answers. Steps 2 to 4 are named in the body and are junior-dev's once he has. | waiting on Donald — card posted to the issue, `BADGE_TABLES` edit reverted |
| 7 | #554 (laterbattle.py's --goto tour charges a leg its whole step allowance, so a run's budget is a fiction and the walk falls through into the pattern walker) | junior-dev | Diagnosis is in the body and corroborated on #334's thread. Fix and a test seen red first. | closed — fixed, reviewed, `6490521` |
| 8 | #15 (Fast Travel for more than one Gold Box title) | reverse-engineering to propose, then stop; emulator-runner and junior-dev after Donald's yes | Curse is done, 24 of 25 named. The forum thread docs/126 cites for Silver Blades holds no area table, so the names come from the game itself: read each of the 22 Silver Blades areas' own script strings, the narration the game prints on arrival, and post a table of proposed names with the string each rests on. Stop there: Donald approves the list. Then the same validation run and landing that Curse had, by the 2026-09-15 comment's grading rule. | in progress — reverse-engineering proposing Silver Blades' 22 area names |
| 9 | #445 (The game's third fight outcome, THE PARTY RUNS AWAY, has never been seen on a screen) | reverse-engineering, then emulator-runner, then close | Core work is done and pushed. Three small follow-ups from the 2026-09-16 comment: what sets $6DE6 in ECL00 (a static read), a driven Curse flee to confirm the wording on screen, and BAR_CANCEL at a live combat bar. The Silver Blades flee waits on item 5. Close with a comment when the three land. | queued — next to dispatch |
| 10 | #516 (Generate boundary characters and check every writer's field widths, since no real save reaches a limit and the corpus cannot find a wrong one) | architect, then junior-dev | The first comment settled Pool of Radiance's spell ceiling at 20, which revises the body's worked example. The generator itself is a subsystem that does not exist, spanning writers and titles, so the architect plans it. Whether hypothesis becomes a dependency is stated in the plan and confirmed by Donald; do not add it without him. | queued |
| 11 | #10 (Finish the high-level test party) | emulator-runner | One fully specified session in the thread settles its four remaining sub-questions and closes the ticket. | queued |
| 12 | #446 (The Amiga saved game's zero argument rests on three of the game's twenty-nine areas) | emulator-runner, then junior-dev | The two saves the thread names, Temple of Bane and Valjevo Castle, then the POR_SAVGAM_UNVISITED table it drafted. Closes when both land. | queued |
| 13 | #89 (Silver Blades' trainer grants spells from a table, and goldbox/levelup.py offers them from a menu) | senior-analyst, then reverse-engineering | The ticket's premise was corrected mid-thread. The analyst reads the correction and goldbox/levelup.py and names what remains: a driven Silver Blades training diff with tools/ssbtrain.py so TRAINER_MEASURED stops excluding that title, and the spell-slot capacity tables read from the binary. | queued |
| 14 | #555 (The DOS driving harness cannot select a spell to memorize or choose which party member a screen acts on) | senior-analyst, then junior-dev | The body says nothing has been designed. The analyst reads tools/dosbox.py and the screens the body names and posts the mechanism. | queued |
| 15 | #449 (The journal reader's rule-book half has never been read off a screen, so 20 of its 50 challenges are untested live) | emulator-runner | The repeat-boot capture experiment the thread specifies. | queued |
| 16 | #500 (What unit is an active effect's duration in, and what do bits 6-7 of the duration byte select?) and #501 (Which active effect ids read their magnitude back when the effect expires, and which discard it?) | reverse-engineering | One agent for both: the same expiry routine answers both and one emulator session covers the two measurements. | queued |
| 17 | #560 (Does Pool of Radiance grant thief abilities off class_bits, or off the level array and skill bytes?) | reverse-engineering | Reachable only on a hand-edited record. The body names two routes; take the disassembly read of the backstab multiplier and thief-command gate, the cheaper one, and answer the question. | queued |
| 18 | #207 (Run an exit's own handler before Fast Travel warps out) | emulator-runner; then architect, then junior-dev | An independent corroboration run first, since the only sample is the author's own. The two-hop case needs a wait-for-idle mechanism across automap/actions.py and automap/window.py that does not exist, so the architect plans it before anything is built. | queued |
| 19 | #249 (Build a DOS party from creation and level it ourselves, so DOS measurements rest on records we watched being written) | junior-dev | Donald ruled on 2026-09-15 that the specimen backup repeats weekly; the comment of that date says what to build. A weekly job runs tools/specimenbackup.py archive into /data/OneDrive/wish-specimens/ with a dated name, then verify. Copy the mechanism that takes the hourly snapshot of work/; if it cannot be found, a systemd user timer, committed under tools/ with a README row. Closes after one unattended firing passes verify. | queued |
| 20 | #323 (The encumbrance identity does not survive the training fee, so failing it is not evidence of an edited record) | reverse-engineering | Sweep Curse's item tables for a bag-of-holding record, the one loose end, then close. | queued |

## Closed, do not touch

#52, #359, #380, #454, #256, #552, #551, #533, #39, #510, #553, #557, #558, #559, #562, #497.

## Do not schedule

#194, #462, #475 (Pools of Darkness), #375 (blocked on the Ultimate), #477, #511, #464, #461, #37, #11, #21
